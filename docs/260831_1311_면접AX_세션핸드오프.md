# 면접 AX — 세션 핸드오프 (2026-08-31 13:11) · 시안 파리티 F11~F13 완료

> ## 새 세션 시작 프롬프트
> 이 핸드오프(`archive/handoffs/` 최신)를 정독하라. **F11~F13(타이포·제목/카드/폭·잔여 정합) 전부 병합·게이트 통과(`VISUAL_PARITY_DONE`)** — 사용자가 고른 후속만 지시서(루트 `260831_0958…`) §8 방식으로. 첫 행동 ListAgents·worktree·codex 확인 + SendMessage 선점. 커밋 로컬만·push 금지. **lg-df 세션이 `app/src/lite/**` 를 소유 중** — 그 경로 불가침, add 는 경로 스코프로만.

## 1. 이번 라운드 (스크린샷 대조 루프)

시안 `design/260831_0656_면접AX_UI개편시안.html` 과 22쌍 페이지 스크린샷 대조 → 3블록으로 소거. **최종 게이트: vitest 794 · e2e 562 ok · FAIL 0 · 크래시 0** (main `80f80d0`).

| 블록 | 내용 |
|---|---|
| F11 | Noto Sans KR 셀프 호스팅(오프라인) 14px 결 전역 재눈금 · 색 규율(폼 컨트롤 ink, 레드는 내비/주 버튼/신호) · 아이브로/제목 아이콘 제거 |
| F12 | 페이지 제목=사이드바 라벨(번호·글리프 제거) · 카드 아이브로 제거 · 포털/명부/회차 960px 기둥 · **데이터 질의 FAB→설정 도움말**(우측 하단 챗봇 하나) · **전체 보기 숨김 + 정적 기본 HR**(`view-role='all'` 센티널로만 전체 보기 진입 — 심사·e2e 용) |
| F13 | 팀 담당자 사이드바 4항목(+파생 배지) · **정적 대시보드 로컬 파생**(스탯4+파이프라인 타일) · 세로 레일 토글 잔재 제거 · 이름 나열 상한(외 n명) · 등록 폼 접기 · 배지 겹침 · 지원자 라벨 통일 · 잔여 가운뎃점(산출물 XLSX 구분자도 쉼표) |

8899(클라우드)·8898(정적) 최종 빌드 가동. 라이트 데모 `/lite.html`.
**[추기 13:2x] L2·L2b(lg-df) 완료 병합**: 라이트 챗봇 = qwen3:8b(Ollama 로컬, think off, 16K ctx) + 화면 데이터 컨텍스트 주입(`src/lite/chatContext.ts`). Ollama 미연결 시 FAB 숨김, 풋 문구 「로컬 LLM(Ollama) 사용, 외부 전송 없음」. main `56c3431`, 재빌드 반영됨. 시연 전 `ollama serve` + `qwen3:8b` 로드 확인.
**[추기 2] L2c~L2e(lg-df, `a74df6b`)**: 워크스페이스 실동작(평가 localStorage + qwen 질문 초안) · 명단 52열 스크롤 · 블라인드 토글(라이트만 기본 ON — 사용자 결정, 본 제품 PRD 기본 미적용 유지) · 사이드바 색 실동작 · 라이트 용어 「전형」(`f83f95e` — 본 앱은 「회차」, 일괄 변경은 사용자 지시 대기). 라이트 테스트 25/25.

## 2. 이번 라운드 계약 추가 (셸·검사)

- 정적 기본 역할 = HR. 전체 보기는 UI 없음 — `localStorage['view-role']='all'` 로만. e2e 본 문맥은 「없을 때만」 이 씨드를 심고, 기본값 검사는 `sessionStorage['e2e-no-role-seed']` 로 옵트아웃.
- e2e SPA 항해 주의: 같은 해시 goto 는 리로드 없음 — 역할 전환은 캡슐 클릭. 명시 역할 set+reload 를 씨드가 덮지 않게 「absent 일 때만」.
- 산출물 XLSX 셀 구분자 쉼표(「이름, 학력」). fs.globSync 금지(Node 20).
- gate.sh 가 `VITEST_FAILED` 마커를 낸다 — 모니터 요약에 반드시 포함.
- 페이지 폭: `NARROW_ROUTES`(960px) vs 전폭(admin·team·interview). FAB 은 도움말 챗봇 하나(assistant.js).

## 3. 남은 것 (사용자 결정 대기)

- 정적 lead 의 포털 「HR 화면/팀 화면」 전환 탭 잔존(심사용 레거시) — 시안은 팀 화면 고정. 잠글지 결정.
- 스크린샷 픽스처: 「샘플 명단」(C-001…)과 「팀 회신 샘플」은 조인 안 되는 별개 세트(결측 467 배지) — 시연은 실제 취합파일.xlsx 경로 사용(기존 시연경로 문서대로).
- 인터랙션 상태 2차 대조(내보내기 열림·모달·슬라이드오버) 미실시.
- FR-4.8 · FR-4.12 · MBTI 공정성 내러티브 — 기존 대기 목록.

## 4. 환경

이전 핸드오프 `260831_1122` §6 그대로. 다음 워크트리 lg-codex37. 페어샷 하네스 `<스크래치>/pair-shots.mjs`(+shots-run.sh) — 시안 대 제품 22쌍 자동 촬영.

VISUAL_PARITY_DONE
